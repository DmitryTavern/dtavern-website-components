//- Props:
//-   - tag: string
//-   - type: string

mixin form-feedback(props={})
	-
		var tag = props.tag || 'div'
		var tagClass = 'form-feedback'
		var tagTypeClass = props.type ? tagClass + '_type_' + props.type : ''

	#{tag}(class=tagClass, class=tagTypeClass)&attributes(attributes)
		block
